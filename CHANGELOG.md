# Changelog

## [0.9.3] - 2026-08-11

0.9.3 正式版汇总了 beta1 至 beta47 的持续测试与优化。这次更新让启动器从页面布局到酒馆管理都更加完整，常用功能更容易找到，重要操作也更安心。

### 页面与上手体验

1. 底部导航调整为“文档、酒馆、启动、工具箱、设置”，版本管理、备份和扩展管理集中到酒馆页的六宫格入口，页面分区更直观。
2. 版本管理、备份、扩展管理和其他管理功能使用独立页面，统一提供左上角返回按钮，也支持手机的返回键或返回手势。
3. 整体换用更清透、耐看的深青薄荷配色；正常操作、提醒和危险操作使用不同颜色，长时间使用更舒服，也更容易分清按钮用途。
4. 文档页改为可连续阅读的使用指南，补充首次使用、多套酒馆、酒馆位置、访问端口、版本操作、权限、迁移和常见问题说明。
5. 首次使用指引、运行状态、任务进度和失败原因都更加清楚，暂时不能点击的功能也会说明还缺少哪一步。

### 启动与版本管理

1. 启动器会自动读取当前酒馆版本，并根据目标版本只显示更新或回退入口；执行前会再次确认当前酒馆、目标版本和操作范围。
2. 更新和回退前会检查酒馆是否停止、是否有文件被手动改过，并保留安全备份；完成后会显示前后版本、处理结果和备份位置。
3. 安装、更新和回退时会打开终端窗口显示实时进度；普通停止酒馆则留在启动器内完成，不再来回跳转。
4. 酒馆运行、启动中、已停止和访问端口被占用等状态识别更加准确；普通停止不会盲目结束无法确认归属的程序，强制清理仍需单独确认。
5. 长时间任务在启动器切到后台或重新打开后仍可继续确认结果，避免重复点击安装、更新、恢复等操作。
6. 发现新的启动器版本时会主动提醒，可直接更新或忽略当前版本，也可以随时查看本机版本的更新内容。

### 备份、恢复与多实例

1. 备份页整合手动备份、自动备份、导入、导出、重命名、删除和备份库；自动备份的间隔与保留数量会持续保存。
2. 每份备份会显示时间、大小和文件位置，并可只读查看角色卡、预设、聊天记录、世界书、正则、扩展、酒馆助手脚本、酒馆美化等内容分类。
3. 内容较多时会按角色卡、预设、脚本来源和聊天对象逐层展开，已有旧备份也会自动补充内容摘要，无需重新创建。
4. 恢复前会明确展示目标酒馆和将要覆盖的内容；兼容的备份可以只恢复用户数据，保留当前酒馆程序与扩展。
5. 新增克隆当前实例，可复制出一套相互独立的酒馆；路径、端口和实例冲突会提前拦截，避免不同实例误用同一目录。
6. 备份、恢复、迁移、更新等重要操作不会同时争抢数据；只有新自动备份成功后，才会按保留规则清理较旧的自动备份。

### 扩展与用户管理

1. 扩展管理支持读取、搜索、安装、启用、停用、检查更新、安全更新、一键回退和删除，并显示名称、版本、作者、大小和所在位置。
2. 扩展安装只接受公开的 GitHub 地址，放入正式目录前会先检查内容；更新失败会尽量恢复原状，发现已有修改时不会直接覆盖。
3. 启用、停用和删除扩展前会确认当前实例与目标位置，并要求先停止酒馆；这些操作不会删除聊天、角色、用户或备份。
4. 酒馆用户管理支持读取用户、新增普通用户和删除账户；删除账户时默认保留用户数据目录，默认用户和最后一名管理员受到保护。

### 设置、修复与稳定性

1. 设置页重新整理酒馆分身、酒馆位置、访问端口、下载来源、权限、用户、修复、诊断和启动器更新，常用内容优先显示，低频选项不再挤满页面。
2. 修复工具提供一键体检、运行环境修复、主题重置、运行内存、聊天文件大小和强制清理；危险操作都会说明影响范围并再次确认。
3. 优化从后台返回后的日志续接和运行状态刷新，大量日志、备份内容或扩展列表不再轻易造成页面卡顿或旧结果混入当前操作。
4. 酒馆位置、访问端口、酒馆分身和任务结果会分开记录，减少切换后显示旧用户、旧扩展、旧状态或把上一次结果套到当前操作的情况。
5. 导入导出、更新下载、备份恢复和扩展操作会先确认下载和文件是否正确，失败时优先保留原有文件和用户数据。

### 升级说明

1. 这是 0.9.3 正式版，汇总并稳定了 47 个测试版中的最终成果。
2. 重要的更新、回退、恢复、迁移或扩展操作前，仍建议先创建一份手动备份。

## [0.9.3-beta47] - 2026-08-11

0.9.3-beta47 版本更新日志：

### 体验优化

1. 普通“停止酒馆”改为在后台执行，确认后会留在启动器等待结果，不再自动切换到 Termux。
2. 停止完成后会直接刷新当前运行状态，仍可在启动页查看操作反馈和运行日志。

### 使用说明

1. 本版本为停止流程跳转体验测试版。
2. 启动酒馆、返回酒馆以及需要查看实时进度的强制清理等操作保持原有行为。

## [0.9.3-beta46] - 2026-08-11

0.9.3-beta46 版本更新日志：

### 新增与优化

1. 底部导航调整为“文档、酒馆、启动、工具箱、设置”，常用功能的归属更清楚。
2. 新增酒馆管理中心，版本管理、备份和扩展管理集中在六宫格入口中；可用入口会显示用途说明，未开放位置明确标记为“敬请期待”。
3. 版本管理、备份和扩展管理改为独立二级页面，统一提供左上角返回按钮，并支持系统返回键或返回手势。
4. 新增工具箱独立分区，先保留清晰的空状态，后续工具可以在这里继续扩展。

### 修复更新

1. 删除启动页重复的“当前状态”总览，酒馆运行状态只在酒馆控制区域展示一次。
2. 统一启动相关确认窗口的标题、说明、操作对象、风险提示和底部按钮顺序，减少弹窗内部零散卡片造成的割裂感。
3. 收紧酒馆管理六宫格的上下留白，放大功能名称，并让整块功能卡都可以点击。

### 使用说明

1. 本版本为页面分区、酒馆管理中心和二级页面导航测试版。
2. 版本更新、回退、备份恢复和扩展操作的原有安全确认与数据保护规则保持不变。

## [0.9.3-beta45] - 2026-08-10

0.9.3-beta45 版本更新日志：

### 新增与优化

1. 备份内容分类改为依次显示“酒馆参数模板、提示词模板、酒馆美化、预设、酒馆助手脚本、角色卡、世界书、聊天记录”；正则和扩展继续保留，并排在这些主要用户内容之后。

### 修复更新

1. 修复全局酒馆助手脚本可能读取不到的问题；用户设置文件较大时仍会在安全上限内读取脚本名称，并兼容新旧设置结构。
2. 升级后会自动重新读取已有备份的内容摘要，无需重新创建备份即可补出全局脚本和新的分类顺序。

### 使用说明

1. 本版本为酒馆助手全局脚本读取和备份内容排序测试版。
2. 内容摘要只读，不会应用、覆盖、移动、修改或删除备份文件。

## [0.9.3-beta44] - 2026-08-10

0.9.3-beta44 版本更新日志：

### 新增与优化

1. 酒馆助手脚本改为多级查看：先区分“全局脚本、预设脚本、局部脚本”，预设脚本继续显示所属预设，局部脚本继续显示所属角色卡，再展开查看具体脚本名。
2. 聊天记录改为按角色卡分组，展开具体角色卡后再显示每份聊天文件；群聊会单独归入“群聊”，不再把角色名和聊天文件混在同一层。
3. 多级内容使用逐层折叠，只有点开当前层级才加载下一层名称，备份内容较多时更容易查找，也不会一次铺满页面。

### 修复更新

1. 删除“提示词模板”下方“包含 instruct、context 和 sysprompt”等说明小字，与酒馆参数模板保持一致。
2. 删除分类内部“还有更多内容未展开列出”的平面截断提示；在安全扫描的前 2000 个备份条目内保留完整名称，并通过角色卡、预设和脚本范围分层展示。
3. 修复酒馆助手脚本只显示脚本名、无法判断来自全局设置、哪个预设或哪张角色卡的问题。
4. 升级后会自动刷新旧版备份内容摘要，已有备份无需重新创建即可生成新的多级结构。

### 使用说明

1. 本版本为备份内容多级查看、酒馆助手脚本来源和聊天记录归类测试版。
2. 如果备份超过 2000 个条目，页面仍会明确提示内容摘要只基于已安全扫描的部分；这不会影响备份文件本身。
3. 内容摘要只读，不会应用、覆盖、移动、修改或删除备份文件。

## [0.9.3-beta43] - 2026-08-10

0.9.3-beta43 版本更新日志：

### 新增与优化

1. 备份内容新增“酒馆助手脚本”和“酒馆美化”分类；可列出用户设置、预设及 JSON 角色卡中的酒馆助手脚本，也可列出用户主题和非空自定义 CSS。
2. 扩展分类会优先读取扩展自己的显示名称，酒馆助手不再只显示为 `JS-Slash-Runner` 文件夹名；普通扩展仍可回退显示目录名。
3. 每份备份的“备份内容”默认整体收起，第一次点击显示角色卡、预设、正则、扩展等分类，再点击具体分类才显示名称，长列表更清爽。

### 修复更新

1. 正则继续按用户正则目录读取，并为扩展名称、酒馆助手新旧脚本格式、嵌套脚本、用户主题和自定义 CSS 增加测试保护。
2. 删除“酒馆参数模板”下方容易造成干扰的说明小字，只保留分类名称、数量和实际文件名。
3. 备份 JSON 深读只针对用户设置、预设、JSON 角色卡和扩展清单，单个文件限制为 2 MB；程序依赖目录不会混入脚本列表，也不会拖慢大备份扫描。
4. 升级后会自动丢弃旧版内容摘要缓存并重新扫描，已有备份无需重新创建即可看到新增分类。

### 使用说明

1. 本版本为备份内容折叠、扩展名称、酒馆助手脚本和酒馆美化识别测试版。
2. PNG 角色卡中的内嵌酒馆助手脚本暂不展开；用户设置、预设和 JSON 角色卡中的脚本可以读取。
3. 内容摘要仍然只读，不会应用、覆盖、移动、修改或删除备份文件。

## [0.9.3-beta42] - 2026-08-10

0.9.3-beta42 版本更新日志：

### 新增与优化

1. 手动备份完成后会自动读取并保存内容摘要；角色卡、预设、正则、聊天、世界书和扩展等分类会长期显示在对应备份的文件大小与地址下方。
2. 旧备份进入备份页后会自动补读一次，成功后直接使用缓存；备份文件大小或修改时间发生变化时会重新读取。
3. 用户管理、扩展管理和后台任务中心的二级窗口统一使用说明卡、状态卡和操作卡，扩展目录复制与搜索也收进同一操作区域。

### 修复更新

1. 修复 NovelAI、TextGen、KoboldAI 参数文件以及 instruct、context、sysprompt 模板全部混入“预设”的问题；这些内容现在会分别显示为“酒馆参数模板”和“提示词模板”，并提示其中可能包含酒馆自带模板。
2. 修复任务中心在空闲状态下比用户管理、扩展管理入口明显更黑的问题，三个管理入口现在使用一致的卡片层级。
3. 删除备份库中多余的“查看内容”按钮和独立只读窗口，内容摘要改为在备份记录中直接折叠查看；“应用并覆盖”的危险确认流程保持独立。

### 使用说明

1. 本版本为备份内容分类、自动摘要和管理界面统一测试版。
2. 第一次进入备份页时，旧备份可能短暂显示“正在自动读取”；完成后摘要会保存，之后无需反复扫描。
3. 内容摘要只读取备份，不会应用、覆盖、移动或删除文件。

## [0.9.3-beta41] - 2026-08-10

0.9.3-beta41 版本更新日志：

### 新增与优化

1. 备份库中的每份备份新增“查看内容”，无需进入覆盖恢复确认即可只读查看角色卡、预设、正则、聊天、世界书和扩展分类。
2. 只读预览会明确说明不会应用、覆盖、移动或删除文件；“应用并覆盖”继续使用独立的危险确认流程。
3. 任务中心入口改为与用户管理、扩展管理一致的标题状态区和“打开”入口；用户与扩展二级窗口统一为说明、状态卡、纵向操作和独立内容卡片。

### 修复更新

1. 修复角色卡目录内部的情绪图片、附带模板和其他嵌套资源被误算成角色卡或预设名称的问题。
2. 角色卡、预设、正则和世界书现在只识别对应用户数据目录的直接子文件，减少备份内容列表混入资源文件名。
3. 用户列表改为随整个二级窗口连续滚动，避免分组标题和长列表互相挤压。

### 使用说明

1. 本版本为设置页层级和备份内容预览修正测试版。
2. 查看备份内容不会修改文件；只有点击“应用并覆盖”并再次确认后才会开始恢复。

## [0.9.3-beta40] - 2026-08-10

0.9.3-beta40 版本更新日志：

### 新增功能

1. 发现新的启动器版本时，进入 App 会自动显示更新提示，可以立即更新，也可以忽略当前版本；以后出现更新的版本仍会重新提醒。
2. 安装、更新和回退酒馆时会自动打开 Termux，实时显示下载、Git 和依赖处理过程，结束后明确提示可以返回启动器继续下一步。
3. 备份恢复预览会按角色卡、预设、正则、聊天、世界书和扩展分类，默认折叠并显示数量，需要时可以展开查看名称。

### 体验优化

1. Termux 命令的展示规则已统一：需要用户查看进度的主动操作会进入前台，状态读取、日志同步和列表查询继续在后台完成。
2. 设置页与后台任务中心统一使用深绿色卡片层级，减少同一页面内板块颜色和层次不一致的问题。
3. 更新提示中的“清除红点”改为更明确的“忽略这个版本”，减少误解。

### 修复更新

1. 修正部分用户主动操作仍在后台静默执行、部分内部查询反而打开 Termux 的不一致。
2. 大型备份预览不再直接展示成千上万个路径；每类名称保留合理上限，避免页面过长或读取内容过多。

### 使用说明

1. 本版本为启动器更新提醒、Termux 前台进度、备份分类预览和设置页视觉统一测试版。
2. 执行恢复、更新或回退前，仍建议先生成一份手动备份。

## [0.9.3-beta39] - 2026-08-09

0.9.3-beta39 版本更新日志：

### 新增功能

1. 扩展管理支持安全更新和一键回退；启用或停用的 GitHub 扩展都可以更新，更新前会保留最近一个可切换快照。
2. 备份恢复新增内容预览，可在确认前查看用户数据、扩展和配置的大致数量；兼容的备份还可以只恢复用户数据，保留当前程序和扩展。
3. 实例管理新增“克隆当前实例”，可把当前酒馆的程序、聊天、角色、扩展、依赖和版本状态完整复制到新的独立分身。
4. 设置页新增“后台任务中心”，可查看耗时任务的进行中或待确认状态、最近结果，并补查 Termux 在切后台期间返回的结果。

### 体验优化

1. 酒馆更新或回退完成后会显示目标版本、前后提交、依赖安装结果和安全备份位置，结果更容易核对。
2. 后台任务中心可以直接跳到任务对应页面；“不再跟踪”只清除启动器记录，不会误称已经取消 Termux 中的任务。
3. 恢复和克隆确认窗口会明确列出操作范围、目标目录和安全限制，长内容可以滚动查看，底部操作按钮保持可用。

### 修复更新

1. 扩展更新改为先在暂存目录验证，再替换原扩展；失败时会自动恢复，且会拦截本地改动、符号链接、路径越界和不受支持的来源。
2. 只恢复用户数据时，仅交换当前酒馆的数据目录；失败会把原数据移回，不会把程序和扩展一起覆盖。
3. 克隆实例会拒绝运行中的酒馆、外部 `dataRoot`、符号链接和非托管目标，复制完成并验证后才登记新分身。

### 使用说明

1. 本版本为扩展更新、选择性恢复、实例克隆和后台任务中心测试版。执行恢复、克隆、更新或回退前，仍建议保留一份手动备份。
2. “只恢复用户数据”仅在备份内容可以确认时提供；不确定时继续使用默认的完整恢复。

## [0.9.3-beta38] - 2026-08-09

0.9.3-beta38 版本更新日志：

### 新增功能

1. 扩展管理新增“检查更新”，可以一次读取当前实例中 Git 扩展的本地提交和远端分支状态。
2. 扩展列表会区分“已是最新、发现更新、检测到本地改动、非 Git 安装、来源不受支持、检查失败”等状态。
3. 对可识别的公开 GitHub 扩展，会显示安全处理后的来源地址以及当前、远端提交短码。

### 体验优化

1. 安装、读取和检查更新使用同一行的紧凑入口；检查更新只读，不需要停止正在运行的酒馆。
2. 单个远端仓库最多等待 15 秒，整批远端检查限制在约 2 分钟内，失效仓库不会无限阻塞其他扩展。
3. 普通“读取扩展”只检查本地 Git 信息，不访问网络；只有点击“检查更新”才会查询远端。

### 修复更新

1. 修正 beta37 中 Git 可用性检查落入无关主题修复函数的位置；主题修复行为未改变，扩展安装和更新检查现在会正确提示 Termux 缺少 Git。
2. 远端来源只有在确认是无凭据、无查询参数的公开 GitHub 地址时才会显示和访问，避免暴露不安全的远端配置。

### 使用说明

1. 本版本为扩展更新检查测试版，只检查并展示状态，不会下载更新、合并提交、移动或删除扩展文件。
2. 检测到本地改动时会明确提示；后续安全更新功能不会直接覆盖这些改动。

## [0.9.3-beta37] - 2026-08-09

0.9.3-beta37 版本更新日志：

### 新增功能

1. 扩展管理新增“安装扩展”，粘贴公开 GitHub 扩展仓库地址后，可以直接安装到当前酒馆实例。
2. 安装确认窗口会显示规范化后的来源地址和准确目标目录，确认后才会开始下载。

### 体验优化

1. “安装扩展”和“读取扩展”使用同一行的紧凑入口，地址输入放在独立确认窗口中，不会把已安装扩展列表整体向下挤。
2. 安装窗口和已经输入的 GitHub 地址在页面重建后会尽量保留，下载失败时也方便重新检查地址。
3. 安装完成后会自动返回最新扩展列表，新扩展默认启用，下次启动酒馆时生效。

### 修复更新

1. 扩展会先下载到启动器专用暂存区，只有 `manifest.json` 存在且有效时才会放入正式扩展目录。
2. 安装会同时检查启用区和停用区，发现同名目录就停止操作，不会覆盖已有扩展。
3. GitHub 地址会在启动器和 Termux 两侧分别校验，并关闭 Git 凭据交互提示，避免错误或私有地址长时间等待输入。

### 使用说明

1. 本版本为 GitHub 扩展安装测试版，目前只接受公开的 `https://github.com/作者/仓库` 地址；安装前必须先停止酒馆并读取一次当前扩展目录。
2. 安装操作不会修改聊天、角色、用户或备份，也不会自动删除已有扩展。

## [0.9.3-beta36] - 2026-08-09

0.9.3-beta36 版本更新日志：

### 新增功能

1. 扩展管理新增“启用”和“停用”操作；不想继续加载某个扩展时，无需直接删除文件，之后也可以随时重新启用。
2. 停用扩展会在酒馆停止后把完整扩展目录移动到启动器专用停用区，重新启用时再原样移回，下次启动酒馆时生效。

### 体验优化

1. 扩展列表现在会明确显示“已启用”或“已停用”，完整路径也会跟随实际存放位置更新。
2. 启用、停用和删除都在扩展管理二级窗口内完成，并在操作前显示当前实例、当前位置和目标位置供确认。
3. 已停用扩展仍会保留名称、版本、作者和大小信息，也可以复制路径或直接删除。

### 修复更新

1. 扩展启停会拒绝路径越界、符号链接停用区和同名目录覆盖；操作失败时不会用目标目录覆盖现有扩展。
2. 兼容旧版启动器返回的扩展列表，旧记录会继续按“已启用”显示，不会因为协议升级而丢失列表内容。

### 使用说明

1. 本版本为扩展可逆启停测试版。启用、停用和删除扩展前必须先停止酒馆；这些操作不会修改聊天、角色、用户或备份内容。

## [0.9.3-beta35] - 2026-08-09

0.9.3-beta35 版本更新日志：

### 新增功能

1. 扩展管理现在会显示每个扩展的完整路径，并支持复制单个扩展路径或扩展根目录；缺少根目录信息时不会伪造绝对路径。
2. 从后台回到启动器时会执行一次更大的日志补读，单次最多接回 256KB，并重新检测酒馆当前真实运行状态。

### 体验优化

1. 启动器切到后台后会暂停高频日志与结果轮询，减少无意义唤醒；回到前台后继续从上次日志游标补读，而不是直接跳到最新位置。
2. 备份页删除重复概览，把当前实例直接放到备份操作标题旁；扩展大小统一显示为 `1.5 MB` 这类带空格的易读格式。
3. 窄屏下“露科亚启动器”标题保持完整单行，过长版本号只在版本胶囊内省略；文档目录增加“左右滑动”提示。
4. 修复工具说明不再强制省略，二级窗口在屏幕旋转或页面重建后会保持打开；运行中限制统一提示“需先停止酒馆”。

### 修复更新

1. 修复首次进入页面时可能把当前实例误判成“已切换”，从而清空刚读取的用户、扩展和上传限制状态的问题。
2. 修复自动备份可能留下两条重复调度链的问题；自动备份执行时只维护自己的任务，不会误取消或重复安排当前任务。
3. 修复普通刷新备份列表时也可能触发自动备份留存清理的问题；旧自动备份现在只会在新的自动备份成功后按规则清理。
4. 修复扩展路径缺少根目录时显示成错误绝对路径的问题，并改善备份页与设置页部分重复或含糊的状态展示。

### 使用说明

1. 本版本为日志续接、备份安全和设置体验测试版。Termux 中的原始酒馆日志不会被删除；执行恢复、更新或回退前仍建议先生成一份手动备份。

## [0.9.3-beta34] - 2026-08-09

0.9.3-beta34 版本更新日志：

### 体验优化

1. 启动器更新内容现在会按“新增功能、体验优化、修复更新、使用说明”等标题分组，版本标题、分组标题和正文使用清楚的三级字号与舒适间距。
2. “发现新版本”和“当前版本更新内容”两个窗口使用同一套更新日志样式，编号会在每个标题下重新开始，更容易快速找到本次新增或修复了什么。
3. 自动备份规则中的时间与保留数量调节按钮增大到至少 48dp 的触控高度，窄屏上仍保持原有文字和布局，但点击更稳。

### 修复更新

1. 修复点击说明感叹号时短暂闪出白色矩形的问题；触控区域继续保持 48dp，但可见圆点和感叹号缩小，不再显得突兀。

### 使用说明

1. 本版本为界面细节与更新说明排版测试版，不会修改酒馆目录、扩展、聊天、角色或备份内容。

## [0.9.3-beta33] - 2026-08-09

0.9.3-beta33 版本更新日志：

1. 修复导入或导出备份时界面被系统重建后，文件选择结果可能丢失的问题；选择状态和目标文件会继续交给当前页面处理。
2. 加固启动器更新下载：下载失败或校验失败不会覆盖原 APK，并会阻止包名不符、版本未提升或签名不一致的安装包。
3. 用户管理移入独立二级窗口，设置页只保留状态和入口；用户较多时使用可滚动列表，默认用户和最后一个管理员都不能删除。
4. 当前标签、扩展管理窗口与搜索条件、新增用户表单、自定义版本输入等临时内容，在界面重建后会尽量保持，不再轻易清空。
5. 优化酒馆安装、更新和回退流程的一致性；安装依赖仍会等待 Termux 包管理锁，并保留配置文件选择、损坏依赖修复及上传限制保护。
6. 优化诊断问题识别，旧的 apt 锁或权限错误在后续出现恢复信号后不再继续误报，单次只展示最有用的四条问题。
7. 说明感叹号保持原来的小图标外观，但扩大了实际触控区域；用户与扩展目录大小也改为一致的 KB、MB、GB 显示。
8. 本版本为备份流程、更新安全、设置页状态和 Termux 操作稳定性测试版；执行恢复、更新或回退前仍建议先生成一份手动备份。

## [0.9.3-beta32] - 2026-08-08

0.9.3-beta32 版本更新日志：

1. 扩展管理从设置主页面移入独立的二级窗口，设置页现在只保留紧凑入口，不再直接铺开扩展目录、读取按钮和完整列表。
2. 入口会显示未读取、读取中、暂无扩展或扩展数量，仍可在设置页快速确认当前状态。
3. 点击“管理已安装扩展”后，可在可滚动窗口中继续读取、搜索和删除扩展；名称、作者、版本、目录和文件大小信息完整保留。
4. 小屏幕下可以滚动访问二级窗口中的全部操作，关闭后会回到原来的设置位置。
5. 扩展删除仍要求先停止酒馆并二次确认；本版本不会修改酒馆路径、聊天、角色、备份或扩展管理的安全规则。
6. 本版本为设置页与扩展管理布局优化测试版。

## [0.9.3-beta31] - 2026-08-08

0.9.3-beta31 版本更新日志：

1. 优化扩展管理列表，读取后会同时显示扩展版本、作者、目录和文件大小，删除前更容易确认目标。
2. 安装多个扩展时新增搜索，可按扩展名称、作者、版本或目录快速筛选，并显示当前结果数量。
3. 没有安装第三方扩展或搜索不到结果时，会显示清楚的空状态说明，不再留下看似没有响应的空白区域。
4. 修复扩展命令返回不完整时仍可能被当成读取成功的问题，避免半截结果误报为空列表。
5. 修复连续读取结果残留时可能把旧扩展和当前扩展合并显示的问题；现在只采用最后一份完整结果。
6. 扩展文件大小扫描会跳过符号链接，删除操作仍只允许处理当前扩展目录下经过确认的普通文件夹。
7. 本版本为扩展管理体验与结果可靠性测试版；读取和搜索不会修改扩展文件，删除仍需停止酒馆并二次确认。

## [0.9.3-beta30] - 2026-08-08

0.9.3-beta30 版本更新日志：

1. 修复 beta29 读取扩展时命令一直等待、约 60 秒后提示没有收到返回的问题；扩展列表与扩展删除现在可以正确执行并返回结果。
2. 同步修复用户管理、依赖修复、主题重置、运行内存、聊天文件大小、恢复和目录迁移等前台操作可能出现的相同超时问题。
3. “诊断与日志”从修复工具弹窗移到设置主页面，导出诊断、清除页面日志和强制清理现在作为独立板块显示。
4. 运行内存的 2GB、4GB、6GB，以及聊天文件大小的 500MB、1GB、2GB 改用紧凑数字按钮；缩小字号和横向留白，并保证完整显示而不是省略号。
5. 继续保留未跟踪进程的停止保护：普通停止不能确认进程归属时不会盲目结束进程，需要时仍通过独立的“强制清理”确认操作。
6. 本版本为扩展管理、前台设置操作和设置页布局修复测试版；不会修改现有酒馆目录、聊天数据、角色、扩展内容或备份。

## [0.9.3-beta29] - 2026-08-07

0.9.3-beta29 版本更新日志：

1. 修复更新到 beta28 后，启动前预检可能提示执行失败并导致酒馆无法启动的问题；较大的启动脚本不再受到系统命令参数长度限制。
2. 启动器安装或更新酒馆命令时会先完整写入临时脚本，再安全替换正式脚本，避免写入中断留下不完整文件。
3. 修复工具弹窗现在按“一键体检、常用修复、运行内存、聊天文件大小、诊断与日志”显示为五个独立板块，底色、边框和间距更容易区分。
4. 本版本为 beta28 启动回归的紧急修复测试版；不会修改现有酒馆目录、聊天数据、角色、扩展或备份。

## [0.9.3-beta28] - 2026-08-07

0.9.3-beta28 版本更新日志：

1. 修复自动备份设置在启动器被划掉后台或进程重新创建后可能恢复默认值的问题；开关、备份间隔和保留数量会独立保存，旧版本设置也会自动迁移。
2. 设置页新增“扩展管理”，可读取当前酒馆实例安装的第三方网页扩展，并显示扩展名称、版本和目录。
3. 支持从启动器删除第三方扩展；删除前必须先停止酒馆，并会再次显示扩展名称、当前实例和完整目标目录。此操作只删除选中的扩展程序目录，不会删除聊天、角色或备份。
4. “启动器更新”区域新增“当前版本更新内容”，检查更新后可直接查看本机已安装版本的完整更新说明。
5. 设置页“修复工具”改为与实例设置一致的紧凑入口；一键体检、依赖与主题修复、运行内存、聊天文件大小和诊断日志统一在点击后的独立页面中操作，不再整块铺满设置页。
6. 本版本为自动备份、更新说明、扩展管理和设置页体验测试版；扩展删除后无法从启动器撤销，需要恢复时请重新安装对应扩展。

## [0.9.3-beta27] - 2026-08-07

0.9.3-beta27 版本更新日志：

1. 降低正文和辅助文字亮度，保留清晰对比但不再接近刺眼的纯白。
2. 降低“启动酒馆”及其他主操作薄荷按钮的亮度，仍保持清凉色相和可读性，长时间使用更舒服。
3. 悬停和按压态只比普通状态稍亮，不再出现荧光感过强的跳变。
4. 本版本只微调前景色亮度，不改变背景、卡片层级、按钮结构、酒馆路径、备份格式、恢复保护或 Termux 命令行为。
5. 本版本为界面舒适度修正测试版。

## [0.9.3-beta26] - 2026-08-07

0.9.3-beta26 版本更新日志：

1. 启动器整体色调从偏灰、偏旧的低饱和绿调整为更清透的冷薄荷；主按钮和选中状态现在更明亮、更有清凉感。
2. 页面背景、卡片和抬升层改用更干净的冷青黑，正文改为冷白，辅助文字减少灰绿色浑浊感，深色页面不再像套着做旧滤镜。
3. 更新与提醒色由暗金调整为明亮柑橘黄，只在小范围提示中出现；停止、删除、覆盖和强制清理继续使用明确的红色危险语义。
4. 继续保持约 6:3:1 的基础色、支撑层级与强调色比例，普通次级按钮仍使用低存在感样式，不会重新变成满屏高亮边框。
5. 本版本为界面色调修正测试版，不改变酒馆路径、备份格式、恢复保护或 Termux 命令行为。

## [0.9.3-beta25] - 2026-07-30

0.9.3-beta25 版本更新日志：

1. 根据真实手机页面重新收敛普通按钮配色；启动、版本、备份和设置页不再让几乎所有操作都显示为薄荷绿。
2. 薄荷绿现在重点用于主按钮、选中状态和关键运行状态；普通次级操作改为清晰的浅色文字与低对比边框，页面主次更容易辨认，也更接近 6:3:1 的视觉比例。
3. 暖金色继续只用于更新、警告和提醒，红色继续只用于停止、删除、覆盖与强制清理等危险操作，颜色语义没有混用。
4. 优化普通按钮的禁用状态，在不可操作时统一降低存在感，避免和可点击操作争抢注意力。
5. 本版本为界面配色修正测试版，不改变酒馆路径、备份格式、恢复保护或 Termux 命令行为。

## [0.9.3-beta24] - 2026-07-30

0.9.3-beta24 版本更新日志：

1. 修复新配色在真实页面中与预览效果明显不一致的问题；卡片、列表和边框不再因为重复透明叠色而变得发黑、发灰或难以区分。
2. 页面重新按约 6:3:1 的视觉比例安排基础深色、抬升层级和强调色；主背景、内容卡片、内层控件、弹窗与底部导航现在各自使用清楚且稳定的颜色角色。
3. Material 组件的完整深色主题已经统一，弹窗、菜单、导航等系统组件不再混入默认的紫灰色或其他未指定颜色。
4. 普通操作继续使用薄荷绿，提醒和更新使用暖金色，红色只用于危险操作；正文、辅助文字、按钮和状态色均通过可读性对比检查。
5. 本版本为配色呈现修正测试版，不改变酒馆路径、备份格式、恢复保护或 Termux 命令行为。

## [0.9.3-beta23] - 2026-07-30

0.9.3-beta23 版本更新日志：

1. 启动器整体换用深青薄荷配色：主背景、卡片、输入框、弹窗和底部导航现在具有更柔和、连贯的深色层级。
2. 主按钮、选中状态和正常运行提示统一使用低饱和薄荷绿，按压与交互反馈会稍微提亮，长时间使用不再显得刺眼。
3. 主要文字改为微绿白，辅助文字改为中灰绿，边框降低对比度；深色页面仍保持清楚的阅读层次。
4. 更新提示、警告和小范围强调统一使用暖金色；删除、覆盖、停止和强制清理等危险操作继续保留明确的红色警示。
5. 本版本为界面配色测试版，不改变酒馆路径、备份格式、恢复保护或 Termux 命令行为。

## [0.9.3-beta22] - 2026-07-30

0.9.3-beta22 版本更新日志：

1. 修复主实例默认酒馆路径与页面说明不一致的问题；首次使用或恢复主实例默认路径时，现在会使用 `~/SillyTavern`。
2. 已经保存的主实例托管路径或自定义路径不会被自动改写，也不会移动现有酒馆文件。
3. 分身实例继续使用 `~/LukoaLauncher/SillyTavern2` 等独立托管目录，避免不同实例共用同一套文件。
4. 主实例仍可手动迁移到 `~/LukoaLauncher/SillyTavern`，分身实例也仍不能占用这个保留目录。
5. 本版本为默认路径一致性测试版，不改变备份格式，也不会自动迁移、覆盖或删除酒馆数据。

## [0.9.3-beta21] - 2026-07-30

0.9.3-beta21 版本更新日志：

1. 修复实例路径或端口因为重复、冲突等原因保存失败时，设置弹窗仍然关闭的问题；现在会留在当前页面并显示失败原因。
2. 保存失败后会保留刚刚填写的路径或端口，方便直接修改，不会突然恢复成旧值。
3. 空白的酒馆路径和迁移目标不再被当成默认目录，避免一次误操作把实例指向非预期位置。
4. 路径和端口会在真正写入前再次校验，即使界面状态发生变化，也不会把无效内容交给保存流程。
5. 本版本为实例设置稳定性测试版，不会自动移动酒馆目录，也不会修改酒馆文件、备份格式或 Termux 命令行为。

## [0.9.3-beta20] - 2026-07-30

0.9.3-beta20 版本更新日志：

1. 设置页按日常使用顺序重新整理：实例与运行环境、用户管理和修复工具优先显示，低频的启动器更新移到页面末尾。
2. 实例设置拆成“当前实例”和“运行环境”两组，路径、端口、镜像源、唤醒延迟与权限不再挤在同一段列表里；修复工具也减少了多层卡片和重复边框。
3. 备份页合并“快速操作”和“自动备份”，概览只保留当前实例、保护状态与备份数量，自动规则只在操作区显示一次，页面更短也更容易找到主要按钮。
4. 修复酒馆尚未检测时主按钮误显示“先安装酒馆”的问题；现在会先提示“先检测酒馆”，只有确认未安装后才引导安装。
5. 普通不可用说明改用清晰的灰色，页面底色与边框层级进一步减轻；正常操作继续使用原有薄荷绿，黄色和红色仍只用于警告与危险操作。
6. 本版本为前端布局与提示修正测试版，不改变备份格式、恢复确认、酒馆路径或 Termux 命令行为。

## [0.9.3-beta19] - 2026-07-28

0.9.3-beta19 版本更新日志：

1. 修复已经结束或放弃跟踪的旧任务稍后返回时，可能误清除新任务状态、覆盖新任务提示的问题；迟到结果现在只能处理它自己对应的操作。
2. 安装、更新、回退、恢复和备份等需要中断恢复的任务，会先确认操作锁和等待记录已经保存，再向 Termux 发送命令；如果记录保存失败，本次操作会安全取消。
3. 修复体检结果不完整时仍显示“当前环境基本正常”的误判；工具、路径、端口等项目没有完整读到时，会明确显示“未确认”并建议重新体检。
4. 切换实例或修改当前酒馆配置后，会清空旧的用户列表和聊天文件大小状态，要求重新读取，避免把旧目录的数据当成当前目录继续操作。
5. Termux 返回中带有其他实例标识时，不再自动覆盖当前实例的页面状态；备份、恢复和版本任务仍可从对应实例的历史结果中继续确认。
6. 本版本为任务安全与状态隔离测试版，不改变酒馆文件内容、备份格式或 Termux 命令本身。

## [0.9.3-beta18] - 2026-07-28

0.9.3-beta18 版本更新日志：

1. 修复长任务执行时划掉启动器后台，再次进入后一直停在执行状态、看不到任务中断提示的问题。
2. 重新进入启动器后会立即显示“任务跟踪已中断”，并说明 Termux 中的原任务可能仍在执行；中断跟踪不代表任务失败。
3. “检查上次操作的结果”只会读取已有返回并刷新页面，不会重新执行安装、更新、恢复或其他原操作。
4. 任务中断提示与绿色执行状态栏保持互斥，避免同一任务同时显示两种互相矛盾的状态。
5. 启动器不会因为重新进入而停止 Termux 原任务，等待记录也会继续阻止重复执行危险操作。

## [0.9.3-beta17] - 2026-07-28

0.9.3-beta17 版本更新日志：

1. 启动页重新整理状态区域：当前状态固定在上方，正在执行的任务或需要确认的上次任务紧随其后，两种状态不会再同时出现或挤到其他操作之间。
2. 修复任务仍在执行时误弹“上次操作需要确认”的问题；只要当前操作锁仍然有效，就继续显示绿色执行状态栏，不会把同一个任务误当成中途未完成。
3. 等待确认的任务卡片进一步缩短，页面只保留任务名称、时间和必要操作，较长说明收进灰色感叹号，普通用户更容易判断下一步。
4. 备份库里的每个手动备份和自动备份都会直接显示格式化后的文件大小与完整文件地址；进入备份页、任务结束或备份增删改后会自动刷新。
5. 本版本为启动状态与备份信息展示测试版，不改变备份内容、恢复覆盖确认或 Termux 命令本身。长任务进行时请不要重复点击操作按钮。

## [0.9.3-beta16] - 2026-07-28

0.9.3-beta16 版本更新日志：

1. “检测到未完成任务”重新设计为“上次操作需要确认”，会明确说明没有收到最终结果不代表操作失败；“检查上次操作的结果”只读取已有结果和刷新状态，不会重新执行更新、恢复或其他原操作。
2. “不再跟踪这次操作”增加二次确认，并清楚说明它不会停止 Termux，也不会删除酒馆、备份或已经生成的文件；任务仍在执行时会提醒不要轻易清除记录。
3. 版本页检测到聊天文件大小相关的本地修改时，会直接说明它可能来自“设置 → 修复工具 → 聊天文件大小”，并提供恢复当前酒馆版本默认值的入口，不再只显示无效的“本地已修改”。
4. 设置页新增“恢复酒馆默认值”，会从当前 SillyTavern 版本读取原本的聊天文件大小，只恢复这一项数值；同一文件里的其他人工修改会保留，操作前也会保存原文件副本。
5. 本版本为任务确认与本地修改处理流程的测试版。更新或回退 SillyTavern 前，如果版本页仍提示其他本地修改，请先按文件列表处理并保留手动备份。

## [0.9.3-beta15] - 2026-07-28

0.9.3-beta15 版本更新日志：

1. 备份条目的“重命名”改为独占一行，修复窄屏手机上按钮只显示“重…”的问题；导出、复制、覆盖和删除入口保持原有用途。
2. 版本页检测到本地修改时，会直接显示改动所在的酒馆目录，并说明先做手动备份、到 Termux 恢复原文件、再回版本页重新检测的处理顺序。
3. 第一次使用指引改为每次只突出一个下一步，并用纵向进度清楚显示 Termux、权限、运行环境和酒馆安装的完成情况；较长说明收进灰色感叹号。
4. 命令执行状态栏重新设计，会显示当前任务、已等待时间、动态处理提示和连续进度条，安装等长任务也会明确提醒无需重复点击。
5. 本版本只优化界面、提示和窄屏可读性，不改变备份恢复、版本保护或 Termux 命令的执行逻辑。这是测试版，建议更新后重点体验新手流程和长任务状态显示。

## [0.9.3-beta14] - 2026-07-28

0.9.3-beta14 版本更新日志：

1. 备份页改为“备份概览、快速操作、自动备份、备份库”的连续流程，删除原来的分区切换，进入页面就能看到全部主要功能。
2. 概览会直接显示当前实例、手动与自动备份数量，以及自动备份规则，切换实例后更容易确认这次正在管理哪套酒馆数据。
3. 生成、导入、刷新和复制文件夹地址重新整理，长按钮改为独占一行；备份条目也明确分开薄荷绿普通操作和红色“应用并覆盖”、删除操作。
4. 手动与自动备份库会连续显示，较多备份默认先展示最近 4 份并允许展开，减少长列表一次占满页面。
5. 自动备份设置不再使用 `--`、`++` 这类难懂按钮，改为按 10 分钟、1 小时和 1 份进行明确调整。
6. 备份目录、自动备份规则、导入导出、恢复预览、覆盖确认、删除确认和数据保护逻辑保持不变。本版本为备份页面体验优化测试版。

## [0.9.3-beta13] - 2026-07-28

0.9.3-beta13 版本更新日志：

1. 版本页改为“当前安装、选择目标版本、准备执行”的连续流程，删除重复的概览和分区切换，不用来回切页就能完成版本选择。
2. 当前版本、目标版本和操作状态不再多处重复显示；提交号、Git 描述和回退点默认收进“技术信息”，普通用户只先看到版本、分支和酒馆位置。
3. 选择较新版本时只显示“执行更新”，选择较旧版本时只显示“执行回退”；按钮不可用时会直接说明还需要停止酒馆、读取版本或处理本地修改。
4. “导出诊断日志”和“清除页面日志”改为各占一行，修复窄屏手机上按钮文字被省略的问题。
5. 自动读取当前酒馆版本、更新与回退确认、安全备份和本地修改保护保持不变。本版本为界面体验优化测试版。

## [0.9.3-beta12] - 2026-07-28

0.9.3-beta12 版本更新日志：

1. 修复工具重新按“先检查问题、常用修复、酒馆运行内存、聊天文件大小、诊断与日志”整理，按钮和当前状态更容易对应，不再依赖大量分割线区分功能。
2. 设置、版本、备份、权限、下载源和用户管理中的感叹号说明改为更适合普通用户的分段表达，补充“什么时候需要用、会影响什么、不知道怎么选时怎么办”。
3. 修复“本地已修改”“检查中”等状态文字与背景颜色过于接近的问题；正常状态保持薄荷绿，真正需要注意的状态继续使用黄色。
4. 本版本只调整界面排版、提示文字和状态可读性，不改变修复、备份、更新、回退或 Termux 命令的执行行为。

## [0.9.3-beta11] - 2026-07-28

0.9.3-beta11 版本更新日志：

1. 设置页继续减少长期占位的黄色说明文字；内存、上传限制、登录标识和强制清理等介绍已收进对应的灰色感叹号说明。
2. 设置项因酒馆运行、其他任务忙碌或正在检查而暂不可用时，点击会显示原因提示，并在短时间后自动消失，不再像没有反应。
3. 6GB 内存和 2GB 上传限制等正常可选操作恢复使用薄荷绿；黄色只用于权限待处理、运行中锁定、检测异常等确实需要注意的状态。
4. 本版本只优化设置页显示与点击反馈，不改变修复、用户管理、强制清理或其他 Termux 操作的执行条件。

## [0.9.3-beta10] - 2026-07-27

0.9.3-beta10 版本更新日志：

1. “停止酒馆”按钮恢复旧版更柔和、耐看的红色，与当前薄荷绿主色搭配更自然。
2. 删除备份、覆盖恢复和强制清理等危险操作仍使用醒目的警示红色，安全提示没有弱化。
3. 本次只调整按钮颜色，不改变停止酒馆的操作方式和行为。

## [0.9.3-beta9] - 2026-07-27

0.9.3-beta9 版本更新日志：

1. 恢复启动器原有的薄荷青绿色主色，不再使用上一测试版偏亮的标准绿色。
2. 正常操作和处理中状态继续使用薄荷绿；白色正文、灰色说明感叹号、黄色警告和红色危险操作保持不变。
3. 本版本只修正界面主色，不会改变启动、版本、备份、恢复或酒馆数据。

## [0.9.3-beta8] - 2026-07-27

0.9.3-beta8 版本更新日志：

1. 启动器主色统一改为绿色，正文继续保持清晰的白色；移除粉色高亮，普通状态和正常操作不再混用警告色。
2. 颜色含义重新统一：正常、可操作和处理中使用绿色，需要处理的问题使用黄色，删除、覆盖、停止和强制清理等危险操作使用红色；所有说明感叹号固定为灰色。
3. 感叹号里的说明改为更适合普通用户的表达，先解释功能会做什么、会影响什么，再保留必要名词；备份、端口、实例、权限、下载源、修复工具和登录标识等说明都已简化。
4. 文档页改为带目录和编号章节的连续阅读布局；备份页删除重复的“数据安全”分区，设置页继续减少长期占位的介绍文字。
5. 覆盖备份、删除数据、目录迁移和强制清理等安全确认保持不变。本版本为界面与文案优化测试版。

## [0.9.3-beta7] - 2026-07-27

0.9.3-beta7 版本更新日志：

1. 设置、备份、权限、镜像、用户管理、修复工具和版本页面进一步精简，重复的功能介绍不再长期占用页面空间。
2. 普通说明统一收进标题右侧的轻量感叹号浮层，图标样式与备份页“快速操作”右侧入口保持一致；点击后不再弹出遮挡页面的大对话框。
3. 覆盖备份、删除、迁移、停止、内存压力等重要状态与风险提示继续保留在正文，不需要主动打开说明才能看到。
4. 文档页改为连续长页面，顶部导航会直接跳到对应章节，不再一次只显示一个分类；新增多实例、路径与端口、安装更新回退、权限顺序、手机迁移和端口排错等内容。
5. 本版本为页面与文档体验优化测试版，不会自动移动、覆盖或删除酒馆数据。

## [0.9.3-beta6] - 2026-07-27

0.9.3-beta6 版本更新日志：

1. 设置页的“当前实例”“酒馆路径”和“实例端口”现在分别打开独立页面，不再反复进入同一个内容过多的设置弹窗。
2. 启动器更新里的“修改仓库地址”和“更新通道”也已分开，点击后只显示当前需要修改的选项。
3. 酒馆路径与实例端口改为独立保存、独立恢复默认；修改其中一项时，另一项会保持当前已保存的值。
4. 实例切换、新建与删除继续集中在实例管理页面；原有目录迁移、删除确认和危险操作保护保持不变。
5. 本版本为设置体验优化测试版，不会自动移动或删除酒馆数据。

## [0.9.3-beta5] - 2026-07-14

0.9.3-beta5 版本更新日志：

1. 继续优化设置页视觉：四个主分区保持不变，内部条目改为更完整的扁平列表，减少重复边框和卡片套卡片，页面阅读更连贯。
2. 版本变化、仓库地址和酒馆路径等长内容改为独占下一行显示；端口、更新通道、镜像状态、权限和体检结果使用更醒目的状态胶囊，窄屏下更容易看清。
3. 用户管理重新整理当前实例提示、读取/新增按钮和用户列表，管理员、目录状态及删除入口更清楚，并继续保留默认用户不可删除的保护。
4. 修复工具按常用修复、Node.js 内存、聊天记录上传限制和诊断日志重新分层；当前上传限制和运行中锁定状态会直接显示，所有原有确认和可用条件保持不变。
5. 一键体检弹窗移除重复标题，并改善整行点击区域和无障碍语义。本版本仅做设置页体验优化，不修改酒馆数据或 Termux 命令逻辑。

## [0.9.3-beta4] - 2026-07-14

0.9.3-beta4 版本更新日志：

1. 设置页在 `0.9.3-beta3` 界面基础上重新整理为“启动器更新、实例管理、用户管理、修复工具”四个分区，原有功能不再分散在多个切换页里。
2. 启动器更新区会直接显示当前版本；发现新版时显示“当前版本 → 目标版本”，并集中提供仓库地址、稳定版/测试版通道、检查更新和打开发布页入口。
3. 实例管理区集中显示当前实例、酒馆路径、端口、网络与镜像源、Termux 唤醒延迟和权限中心；用户管理区会明确标出当前正在管理的实例。
4. 一键体检、npm 修复、主题重置、Node.js 内存、聊天记录上传限制、强制清理和诊断日志统一归入修复工具，功能和原有安全确认保持不变。
5. 修复酒馆运行状态变化后，用户修改或修复按钮可能因旧体检结果而错误开放的问题；现在会按当前实时运行状态锁定危险操作。本版本为界面整理测试版。

## [0.9.3-beta3] - 2026-07-14

0.9.3-beta3 版本更新日志：

1. 撤回 `0.9.3-beta1` 和 `0.9.3-beta2` 的新版界面，页面布局、导航和操作方式恢复到 `0.9.2-beta12`。
2. 酒馆用户读取、登录标识说明、多实例、备份、上传限制和修复工具等 `0.9.2-beta12` 已有功能保持不变。
3. 本次只回退启动器界面与同期改动，不会回退或删除酒馆数据、聊天记录、角色、配置和备份。
4. 版本号继续递增，已安装 `0.9.3` 测试版的用户可以直接覆盖更新。本版本为界面回退测试版。

## [0.9.2-beta12] - 2026-07-13

0.9.2-beta12 版本更新日志：

1. 修复托管目录和其他路径下读取酒馆用户失败的问题：离线用户工具现在会先设置当前 ST 的 `config.yaml`，再加载官方用户模块。
2. 新增用户页面进一步说明“登录标识”：它是登录时使用的英文短名，也是 `data/登录标识` 数据目录名，不是页面显示昵称。
3. 继续明确登录标识创建后不能在启动器里重命名，因为 SillyTavern 官方没有提供对应接口。本版本为测试版。

## [0.9.2-beta11] - 2026-07-13

0.9.2-beta11 版本更新日志：

1. 用户管理移除重命名入口，不再提供容易让账户记录和数据目录产生误解的改名操作。
2. 新增用户对话框会明确提示：登录标识会成为数据目录名，SillyTavern 官方没有提供修改登录标识的接口，创建后不能在启动器中重命名。
3. 用户创建、列表读取和“删除账户但保留数据目录”功能保持不变。本版本为测试版。

## [0.9.2-beta10] - 2026-07-13

0.9.2-beta10 版本更新日志：

1. 设置页新增“酒馆用户管理”，用于管理当前酒馆内部的用户账户，与启动器多实例分开显示。
2. 支持读取用户列表、目录是否存在和目录大小，并可新增普通用户。
3. 支持删除用户账户但默认保留数据目录，避免聊天、角色和设置随账户删除而丢失。
4. 默认用户禁止删除；删除管理员前会确保至少保留一个启用的管理员。
5. 用户修改要求酒馆停止，输入会限制登录标识格式和显示名称长度，切换启动器实例后会清空旧用户列表。
6. 登录标识暂不支持直接改名，避免账户数据库和数据目录错位。本版本为测试版。

## [0.9.2-beta9] - 2026-07-13

0.9.2-beta9 版本更新日志：

1. 修复 Node.js 内存设置可能覆盖用户已有 Node 启动参数的问题，现在会保留其他参数，并只读取经过校验的 2GB、4GB、6GB 配置。
2. 修复内存配置读取方式可能执行异常配置内容的问题，不再把配置文件当作 shell 脚本加载。
3. 主题重置现在必须唯一识别一个用户设置文件；多用户或目录异常时会停止并列出候选，避免修改到错误用户。
4. npm 依赖修复失败时会先隔离本次失败目录，再恢复原依赖，并在反馈里说明是否恢复成功。
5. 上传限制补丁备份改为只保留当前有效副本，减少多次修改、更新或回退后持续占用存储空间。
6. 增加修复脚本回归检查，防止配置分支结束符、更新保护和危险读取方式再次写错。本版本为测试版。

## [0.9.2-beta8] - 2026-07-13

0.9.2-beta8 版本更新日志：

1. 修复上传限制检查结果只出现在操作日志、修复工具页面没有显示当前值的问题。
2. 页面现在直接显示当前上传限制、是否由启动器管理，以及补丁是否可能被 ST 更新或手动修改覆盖。
3. 修复上传限制修改后可能阻止 ST 更新或版本回退的问题：操作前安全验证并临时恢复原文件，完成后自动重新应用用户选择。
4. 如果目标文件除上传数值外还有其他修改，启动器会拒绝自动覆盖，避免误删用户自己的源码改动。
5. 修复工具统一增加操作锁，避免连续点击重复发送依赖修复、主题重置、内存或上传限制命令。本版本为测试版。

## [0.9.2-beta7] - 2026-07-13

0.9.2-beta7 版本更新日志：

1. 修复工具新增聊天记录上传限制设置，可选择恢复 500MB、调整为 1GB 或调整为 2GB。
2. 支持先检查当前限制；只有识别到当前 SillyTavern 中唯一且兼容的上传中间件时才允许修改，避免误改其他源码数字。
3. 修改前会保留原文件副本，并记录原值、新值和当时的 SillyTavern 提交版本。
4. SillyTavern 更新完成后会自动检查补丁状态，提示限制仍然生效，还是已被上游更新覆盖。
5. 大文件会显著增加 Termux 内存和处理时间，1GB 以上限制请根据设备内存谨慎使用。本版本为测试版。

## [0.9.2-beta6] - 2026-07-13

0.9.2-beta6 版本更新日志：

1. 设置页新增“修复工具”，可在当前酒馆实例停止后安全重装 npm 依赖；安装失败时会尽量恢复原依赖目录。
2. 新增网页主题重置，可在错误主题导致页面打不开时恢复为 Dark Lite，并在修改前保留原设置文件副本。
3. 新增 Node.js 内存上限设置，可选择 2GB、4GB 或 6GB；配置按酒馆实例独立保存，不直接修改用户的启动脚本。
4. 所有修复操作均增加停止状态检查、执行确认和更长的 Termux 等待时间。本版本为测试版，请在重要操作前保留数据备份。

## [0.9.2-beta5] - 2026-07-11

0.9.2-beta5 版本更新日志：

1. 修复部分手机划掉启动器后台后，重新进入 App 仍保留上一次酒馆运行日志的问题。
2. 新任务重进现在按页面任务是否重新创建判断，不再依赖 App 进程是否被系统彻底结束，适配划掉任务但进程仍存活的情况。
3. 清理显示后，第一批离线期间积累的旧日志不会重新出现在页面上，但仍会完整写入后台诊断归档，并继续用于运行状态判断。
4. 设置中的手动“清除日志”现在也只清空页面显示，不删除后台记录，不影响后续导出日志和诊断。
## [0.9.2-beta4] - 2026-07-11

0.9.2-beta4 版本更新日志：

1. 优化酒馆运行状态同步：无论酒馆是在启动器内启动，还是在 Termux 等其他位置启动或关闭，启动器都会更快切换到对应状态。
2. App 被划掉后台后重新打开，会在约 300 毫秒后主动校准当前实例状态，不再长期沿用关闭前的旧显示。
3. App 从后台回到前台时，会额外执行一次轻量运行状态检测，不重复读取版本或检查更新。
4. 外部启动的酒馆在网页可访问后会自动显示为“运行中”；网页尚未准备好时显示为“启动中”并保留停止入口；外部关闭后自动恢复为“未运行”。
## [0.9.2-beta3] - 2026-07-10

0.9.2-beta3 版本更新日志：

1. 修复酒馆已经进入启动流程、但网页还没准备好时，主按钮错误变回“启动酒馆”或停止入口消失的问题。
2. 启动过程中主按钮会保持为红色“停止酒馆”，用户无需等待启动超时即可主动停止当前实例。
3. 实时日志状态判断只读取启动器生成的顶部状态信息，不再让聊天内容或运行日志中的 JSON 字段误改酒馆的启动、运行或停止状态。
4. 停止确认入口现在同时支持“运行中”和“启动中”，减少重复启动以及无法及时中止启动流程的情况。
## [0.9.2-beta2] - 2026-07-10

0.9.2-beta2 版本更新日志：

1. 修复酒馆已经停止、端口也没有监听时，实时日志同步仍可能把残留的 Termux 命令进程误判成酒馆正在运行的问题。
2. 加强酒馆进程身份校验，现在会同时核对进程工作目录、实际可执行程序和精确启动参数，不再只凭命令文本里出现 `server.js` 或 `start.sh` 判断。
3. 实时日志只负责同步日志和确认停止状态，不再仅凭候选进程把已停止的实例重新标记为运行中。
4. 清理失效或已经被其他进程复用的旧 PID 记录，减少停止、迁移、更新和回退被错误拦截的情况。

## [0.9.2-beta1] - 2026-07-10

0.9.2-beta1 版本更新日志：

1. 清理已经停用且没有调用入口的旧界面流程，减少后续功能更新时误接回旧安装、权限、状态或备份交互的风险。
2. 同步项目模块导航与当前代码结构，后续修复会更容易定位到正确功能区域，降低改动波及其他页面的可能。
3. 清理开发过程中留下的临时补丁、发布草稿和本地依赖目录，并增加忽略规则，避免这些文件再次混入版本改动。
4. 当前启动、安装、权限、版本管理、备份恢复和多实例操作方式保持不变。

## [0.9.1] - 2026-07-10

0.9.1 正式版汇总了 beta1 至 beta31 的测试与优化：

1. 新增并完善多实例管理，可为不同实例分别设置酒馆目录和端口，运行状态、PID、日志、版本与回退记录按实例隔离，减少切换实例后串状态或误操作。
2. 完善酒馆目录识别与迁移，支持传统目录、启动器托管目录和自定义目录；迁移、删除和选择候选目录时会检查运行状态、路径占用与实例冲突，并保留清楚的危险操作确认。
3. 加固备份与恢复流程，增加恢复预览、备份名称与路径校验、后台读取和导出、大文件防卡顿、超时归档补识别，以及自动备份与其他高风险操作之间的互斥保护。
4. 统一安装、更新和回退确认，明确当前版本、目标版本和 Git 源；切换源后会重新读取对应版本列表，减少错源更新、错误回退和版本信息混用。
5. 优化首次启动、Termux 唤醒、后台常驻和权限引导，针对命令已发送但 Termux 未打开、后台被系统限制等情况给出更准确的下一步提示，减少重复启动。
6. 区分温和停止与强制清理。普通停止不再自动升级为强制清理；端口占用或残留进程需要用户从诊断入口明确确认，降低多实例下误杀进程的风险。
7. 改善酒馆运行日志与诊断体验，日志保留 ANSI 颜色并减少重复、截断和无关包裹内容；日志和诊断导出改为后台处理，大日志下更不容易卡住界面。
8. 长时间安装、更新、回退、备份、恢复和目录迁移使用持久化操作锁、独立命令标记和未完成任务恢复，App 重建后仍能继续检查，减少旧结果串入新任务或按钮提前解锁。
9. 优化端口占用、路径同址、候选目录、运行状态和当前实例识别，减少把其他进程误判为酒馆、把旧实例结果套到当前实例或把临时端口输入误保存的问题。
10. 对话框、备份、实例路径和镜像设置的状态进一步隔离，现有按钮、确认方式、默认 `~/SillyTavern` 和用户自定义路径保持不变。

## [0.9.1-beta31] - 2026-07-10

0.9.1-beta31 版本更新日志：

1. 优化多实例、酒馆目录和镜像设置的状态隔离，切换实例、保存路径或检测镜像时更不容易与其他页面状态互相影响。
2. 目录候选选择、自定义目录迁移、托管目录迁移和实例删除继续使用统一的确认与未完成任务保护，降低复杂操作重复触发或状态错位的风险。
3. Git、npm 和 Termux 包源的保存、检测与结果反馈统一管理，切换 Git 源后仍会清空旧版本列表，避免继续使用不匹配的版本信息。
4. 保留默认 `~/SillyTavern`、自动读取当前酒馆版本、用户自定义路径和现有危险操作确认流程，更新后操作方式不变。

## [0.9.1-beta30] - 2026-07-10

0.9.1-beta30 版本更新日志：

1. 优化备份页面的状态隔离，自动备份设置、列表刷新、恢复预览和文件操作不再与其他页面状态混在一起，降低复杂操作时界面互相影响的风险。
2. 恢复预览的请求、加载任务和取消状态统一管理，切换备份或关闭加载提示时更不容易残留旧请求。
3. 复制、导出、导入、重命名和删除备份统一使用同一套路径校验、忙碌拦截和结果反馈，减少后续功能调整时出现行为不一致的可能。
4. 保留现有恢复确认、存储权限重试、持久化操作锁和未完成任务恢复流程，更新后操作方式不变。

## [0.9.1-beta29] - 2026-07-10

0.9.1-beta29 版本更新日志：

1. 自动备份运行期间会暂时锁定恢复、更新、迁移和其他可能改动酒馆数据的操作，避免多个高风险任务同时执行。
2. 自动备份完成、失败或发送失败后只会释放属于自己的操作锁，不会误解除之后开始的其他任务。
3. 自动备份写入新归档期间不会提前清理旧备份，待备份完成并确认成功后再执行保留数量清理。
4. 复制、重命名和删除本地备份等操作现在也会进入统一的忙碌状态，减少与后台自动备份互相覆盖的风险。

## [0.9.1-beta28] - 2026-07-10

0.9.1-beta28 版本更新日志：

1. 修复 App 重启后继续检查安装、更新、回退、备份、恢复和目录操作时，可能把更早的同类 Termux 返回误认为本次结果的问题。
2. 每次发送到 Termux 的命令现在都会带独立身份标记，并持久记录递增执行编号，降低冷启动或旧命令延迟返回时串结果的风险。
3. “继续检查”会从近期结果记录中寻找任务开始之后的匹配返回，不再因为最新一条是状态检测或其他命令，就漏掉已经完成的高风险操作结果。

## [0.9.1-beta27] - 2026-07-10

0.9.1-beta27 版本更新日志：

1. 优化日志、备份恢复、权限、更新、启动停止、多实例和版本管理等对话框的内部组织，降低复杂操作之间界面状态互相影响的风险。
2. 现有确认文案、按钮、关闭方式和危险操作确认流程保持不变，更新后不需要重新适应操作方式。
3. 改善后续界面问题修复和功能更新的一致性，便于按功能区域独立验证，减少改动波及其他页面的风险。

## [0.9.1-beta26] - 2026-07-10

0.9.1-beta26 版本更新日志：

1. 酒馆运行日志现在保留 Termux 输出中的 ANSI 颜色，并在启动器内按标准色、256 色和 RGB 真彩色正常显示，不再出现转义字符乱码。
2. 酒馆运行日志页面不再额外隐藏较早行，模型列表、扩展列表和其他原始输出会按日志内容完整显示；导出的酒馆运行日志也会保留原始颜色控制信息。
3. 安装、更新、回退、备份、恢复、目录迁移等长任务统一使用结构化命令解析、结果等待超时和持久化操作锁，减少 App 重建或等待边界处提前解锁、重复操作的风险。
4. 停止酒馆、读取当前版本和读取官方版本统一走内置脚本实现，移除启动器中已废弃的重复 Shell 路径，降低后续行为不一致的风险。


## [0.9.1-beta25] - 2026-07-10

0.9.1-beta25 版本更新日志：
1. 长时间安装、更新、备份或恢复期间，即使界面被系统重建，启动器也会继续显示原来的处理中状态，避免按钮提前恢复后重复发送命令。
2. 恢复出的操作等待会保留原有经过时间，并在剩余等待结束后自动解除，不会从头重新计时。
3. 检测到上次未完成任务时，“继续检查”和“放弃记录”仍可正常使用；确认任务已结束或主动放弃后，会同步解除旧的操作限制。
4. 酒馆运行日志不再省略模型列表和扩展列表，并扩大页面保留范围，查看内容更接近 Termux 里的原始运行日志。
5. “只导出 Termux 侧”调整为“只导出酒馆运行日志”，导出文件不再混入命令回传内容。

## [0.9.1-beta24] - 2026-07-10

0.9.1-beta24 版本更新日志：
1. 打开备份恢复确认时，备份名称、时间和大小会改在后台读取，减少读取外部备份信息时界面卡顿或无响应的情况。
2. 读取备份信息期间会显示明确提示并阻止重复操作；如果备份无法读取，不会进入恢复确认流程。
3. 当前选中的酒馆实例正在运行或启动时，版本管理会直接阻止更新和回退，并提示先停止当前实例，减少误操作风险。

本文件只记录对外可感知、或会影响开发流程的重要变化。

## [0.9.1-beta23] - 2026-07-10

0.9.1-beta23 版本更新日志：
1. 优化安装、更新、回退、备份和恢复等长任务的结果等待方式，减少任务执行期间持续卡顿和掉帧的情况。
2. Termux 返回结果现在会在后台完成保存和后续整理，命令完成后的反馈更稳定，也减少了返回瞬间界面无响应的风险。
3. 收紧诊断历史的体积并去除重复输出，在保留冷启动恢复和问题排查能力的同时，降低长期使用后的存储与解析负担。

## [0.9.1-beta22] - 2026-07-10

0.9.1-beta22 版本更新日志：
1. 修复历史日志较大时，导出运行日志或诊断日志可能长时间卡住、界面无响应的问题。现在会直接读取最近的有效日志，不再先把整份历史文件加载进内存。
2. 超大日志被截断时会在导出文件里明确说明，并保留最近的日志内容，既方便继续排查，也能减少内存占用和闪退风险。

## [0.9.1-beta21] - 2026-07-10

0.9.1-beta21 版本更新日志：
1. 启动页移除了“Termux 前台回传”窗口，日志区域现在只保留真正需要查看的酒馆运行日志，页面更直接。
2. Termux 底层回传仍会用于状态判断、诊断和导出，不会因为窗口移除影响启动、检测或问题排查功能。

## [0.9.1-beta20] - 2026-07-09

0.9.1-beta20 版本更新日志：
1. 启动页里的 Termux 区块继续收口，显示内容会更接近你在真正 Termux 前台看到的当前启动会话，而不是把整段酒馆运行日志混在一起顶上来。
2. 运行日志导出链路改到后台处理，日志较大时不该再因为生成导出文件把界面直接卡死。
3. 导出的日志内容也重新分段整理了，Termux 前台回传、酒馆运行日志和 App 操作反馈会更容易分开看，排查问题时少一点对不上号的困惑。

## [0.9.1-beta19] - 2026-07-09

0.9.1-beta19 版本更新日志：
1. “强制释放端口 / 清理残留进程”入口已挪到设置页的“诊断与日志”里，不再和启动、停止按钮混在一起，排查问题时更容易按用途去找。
2. Termux 调用返回里不再显示 `==== SillyTavern live log ... ==== ` 和 `==== end SillyTavern live log ==== ` 这类包裹标记，界面会直接保留里面真正的日志内容。
3. 如果当前没检测到需要强制清理的残留进程，入口提示也会更直接，少一点“点了没反应”的困惑。

## [0.9.1-beta18] - 2026-07-08

0.9.1-beta18 版本更新日志：
1. Termux 调用返回现在尽量按原样显示，不再额外加时间戳，也不再拆成“实时新增”“新增日志”“最新重要日志”这类二次标签，排查问题时更容易直接对照 Termux 里的内容。
2. 普通命令返回和后台日志同步现在统一按同一种展示方式处理，减少同一段输出被重复整理、截断或换一种说法后看不明白的情况。
3. “强制释放端口 / 清理残留进程”入口现在会固定显示，不再只在特定状态下偷偷出现，想手动处理残留进程时更容易找到。

## [0.9.1-beta17] - 2026-07-08

0.9.1-beta17 版本更新日志：
1. 停止酒馆现在默认只做温和停止，不会再在普通停止里悄悄升级成强制清理，减少多实例场景下误杀残留进程的风险。
2. 新增独立的“强制释放端口 / 强制清理残留进程”入口。只有普通停止没退干净，或明确检测到当前实例残留进程、端口占用时，才会提示你手动确认继续。
3. 强制清理前的确认信息补得更清楚了。现在会把当前实例、当前目录、当前端口，以及为什么建议强制清理写出来，方便你先确认不会清到别的实例。
4. 启动前预检、状态摘要和相关提示也一起收口，普通停止、残留进程和端口占用的区别会更直白，少一点“到底该点哪个按钮”的困惑。

## [0.9.1-beta16] - 2026-07-08

0.9.1-beta16 版本更新日志：
1. 修复多实例下长任务容易串结果的问题。安装、更新、回退、备份、恢复这类操作，现在会优先按当前实例识别回传结果，减少切到别的分身后把旧任务结果认到当前实例上的情况。
2. 修复端口占用时的状态误判。现在如果酒馆端口其实是被别的进程占用，启动页、状态摘要和一键体检会直接提示端口冲突，不再误显示成“酒馆正在运行”或错误引导你去点“停止酒馆”。
3. 优化路径防呆。`~/`、`$HOME/`、带 `.` / `..` 或重复斜杠的目录现在会更稳地按同一个路径识别，减少分身实例误碰主实例保留目录或目录同址识别漏判的问题。
4. 修复“选择检测到的目录”会误带未保存端口输入的问题。现在切到检测到的目录时，会保留当前实例已经保存好的端口，不会顺手把临时输入框里的端口一起带过去。

## [0.9.1-beta15] - 2026-07-08

0.9.1-beta15 版本更新日志：
1. 启动前的预检弹窗现在会直接显示当前实例、当前目录和当前端口，点启动前就能先肉眼确认这次准备启动的是哪一个实例，减少误启动到别的目录的担心。
2. 这次显示的是当前启动器已经选中的实例配置，不会因为 `start.sh` 文件名相同就自动跑去主实例，方便你在处理权限、路径或环境问题时顺手一起确认目标实例有没有选对。

## [0.9.1-beta14] - 2026-07-08

0.9.1-beta14 版本更新日志：
1. 修复分身实例误把主实例托管目录当成候选目录、迁移目标甚至启动目录的问题。`~/LukoaLauncher/SillyTavern` 现在会一直保留给主实例，分身实例默认继续从 `~/LukoaLauncher/SillyTavern2` 往后分配。
2. 补上启动前防呆：如果当前实例路径误指到主实例保留目录，启动器会先拦住并提示去改路径，避免分身实例启动到主实例的酒馆。
3. 修复一类“备份明显变慢、长时间不结束”的问题：如果酒馆把 `dataRoot` 配成了上级大目录，备份现在会直接阻止并说明原因，不再傻等很久。
4. 优化手动备份超时后的兜底处理：如果备份文件其实已经生成，只是 Termux 最终返回丢了，启动器会自动补识别并把备份补回列表，减少白等十分钟后还以为失败的情况。
5. 补强路径同址识别：`~/...`、`$HOME/...` 和 Termux 绝对路径现在会按同一个目录处理，减少迁移后按钮状态不对、默认目录识别失准和实例路径占用漏判的问题。

## [0.9.1-beta13] - 2026-07-08

0.9.1-beta13 版本更新日志：
1. 修复实例迁移后的路径识别串状态问题：只有“目录不存在”确实还指向当前实例当前路径时，才会再弹候选目录；旧实例或旧路径留下的结果，不该再把你刚迁好的实例重新拉去选目录。
2. 优化路径设置里的实例刷新。切换实例、迁移目录成功，或删除分身实例后，路径设置会按当前实例重新刷新路径类型和迁移按钮状态，减少还要切去别的实例再切回来的情况。
3. 修复迁移到新目录后仍可能被误判成“酒馆还在运行”或“还在启动”的问题，收紧进程识别条件，减少旧目录残留或无关进程导致的假运行状态。

## [0.9.1-beta12] - 2026-07-08

0.9.1-beta12 版本更新日志：
1. 酒馆默认安装目录改成启动器托管位置。主实例默认会用 `~/LukoaLauncher/SillyTavern`，新建分身实例也会自动分配各自的独立目录，减少和旧目录、反代目录混在一起的风险。
2. 设置页新增酒馆目录迁移入口，支持把当前实例迁移到启动器托管目录、主实例的传统默认目录 `~/SillyTavern`，或你自己填写的自定义地址；迁移前会先拦住运行中实例、重复目录和实例冲突目录。
3. 优化删除分身实例的防呆：如果分身实例正在使用它自己的托管默认目录，删除实例时会明确提示“会连目录一起删除”；如果是自定义目录，则仍然只删启动器配置，不会乱删文件。
4. 手动备份和自动备份的默认下载目录统一改到 `Download/LukoaLauncher/backups/sd` 和 `Download/LukoaLauncher/backups/zd`，旧的 `Download/lukoa/backups` 不再继续兼容，也不再回退到 Termux 私有备份目录。

## [0.9.1-beta11] - 2026-07-08

0.9.1-beta11 版本更新日志：
1. 修复多实例下的目录识别问题：候选目录现在会避开明显不像 SillyTavern 的目录，也会拦住已经被别的实例占用的目录，减少把反代目录或主实例目录误当成当前分身目录的情况。
2. 修复切换实例后启动页仍沿用旧实例状态的问题。现在切换、新建、删除实例，或修改当前实例路径和端口后，会立刻按当前实例刷新运行状态、版本状态和安装状态。
3. 修复导出诊断日志时容易把界面卡住的问题。诊断文件现在改成后台生成，日志较大时不该再把 App 卡死。

## [0.9.1-beta10] - 2026-07-08

0.9.1-beta10 版本更新日志：
1. 修复切换实例后启动页仍沿用旧实例状态的问题。现在切换、新建、删除实例，或修改当前实例路径和端口后，会立刻按当前实例刷新运行状态、版本状态和安装状态。
2. 修复实例删除里的高风险缺口：如果当前分身实例还在运行或启动中，启动器现在会先拦住，避免删掉配置后找不回这个实例的运行状态。
3. 新增实例删除确认框，删除前会把实例名、目录、端口，以及删除后会切回哪个实例写清楚，并明确提示这一步不会删除酒馆目录和备份。

## [0.9.1-beta9] - 2026-07-08

0.9.1-beta9 版本更新日志：
1. 修复实例管理里的高风险问题：如果旧配置里已经把主实例删没了，启动器现在会自动把实例配置扶正回来，不再容易卡在“只剩分身、恢复不回来”的状态。
2. 优化实例删除防呆：主实例现在默认保留，只有切换到对应分身实例后，才允许删除那个分身，减少误删后难恢复的情况。
3. 补充实例管理提示文案，让“当前为什么不能删、应该先切到哪里再删”这类信息更直白，少走弯路。

## [0.9.1-beta8] - 2026-07-08

0.9.1-beta8 版本更新日志：
1. 设置页现在可以按实例管理酒馆目录和端口，支持切换当前实例、新建分身实例，以及分别修改当前实例的路径和端口，给多实例使用打好了入口。
2. 优化自定义端口下的启动和识别链路：启动器会按当前实例端口去打开网页，首次启动成功识别和端口冲突识别也不再只盯着 `8000`。
3. 修复多实例下容易互相串状态的问题：Termux 的运行状态文件、PID、日志和回退记录现在会按实例分开保存，减少切换实例后读错状态、停错进程或串日志的风险。

## [0.9.1-beta7] - 2026-07-07

0.9.1-beta7 版本更新日志：
1. 优化启动前体检里的端口占用提示：如果酒馆端口已被别的进程占用，会优先提醒先处理端口，不再跳到后台权限等后续操作，避免用户误以为可以直接启动。
2. 优化启动酒馆前的预检说明：遇到端口占用时，会明确建议先关闭占用端口的应用，或重启 Termux/手机后重新体检。
3. 补充 Termux 状态推断测试，覆盖“正在启动”“前台会话退出”“已有进程但网页暂时打不开”等边界，减少重复启动或误判停止的风险。

## [0.9.1-beta6] - 2026-07-07

0.9.1-beta6 版本更新日志：
1. 进一步加固 Termux 唤醒链路：只有系统真正打开 Termux 后，才会记录唤醒冷却，避免唤醒失败后短时间内再点按钮却被误提示“刚刚已经触发过”。
2. 优化启动酒馆时的异常提示：启动命令已发出但系统没有自动打开 Termux 时，会明确提醒手动打开 Termux 查看日志，并提示先不要重复点启动。
3. 补充启动状态摘要测试，确保这种“命令已发送但需要手动打开 Termux”的场景仍被视为启动中，减少用户重复启动造成双进程的风险。

## [0.9.1-beta5] - 2026-07-07

0.9.1-beta5 版本更新日志：
1. 优化“唤醒 Termux 并返回”按钮，冷却中、正在唤醒、未找到 Termux 等情况会给出更准确的提示，不再只显示笼统的成功或失败。
2. 优化从后台切回启动器的链路：当 Termux 已安装、RUN_COMMAND 可用、但 Termux 后台常驻还没放行时，启动器会按冷却规则自动唤醒 Termux 并返回，减少后台被系统收掉后用户还以为酒馆能继续启动的情况。
3. 优化第一次启动酒馆前的非 iQOO 引导，按钮改为“唤醒 Termux 并返回”，会先打开 Termux 再回启动器，回来后再继续启动更顺。
4. 优化 iQOO 首次启动引导，Termux 后台常驻已经放行时不再重复弹出授权提醒。
5. 优化首次启动成功历史识别，已经检测到酒馆运行或网页可访问时，不再反复弹出首次启动引导。
6. 补充权限中心和体检里的 Termux 后台常驻说明，让首次启动酒馆、长任务、自动备份和前台日志同步的风险提示更清楚。

## [0.9.1-beta4] - 2026-07-03

0.9.1-beta4 版本更新日志：
1. 新增首次启动酒馆前的新手引导：iQOO 会提醒先给 Termux 开后台，其他手机会提醒先把 Termux 挂到小窗或分屏，减少第一次启动就被系统打断的情况。
2. 优化设置页里的权限展示，改成轻提示和详情入口，真正的运行状态继续集中放在启动页，界面更清楚。
3. 优化权限中心和诊断日志，补充 Termux 后台常驻检测，长任务、前台日志和自动备份遇到系统省电限制时更容易看出问题点。
4. 修复设置页状态信息塞得太满、打乱整体美化和阅读节奏的问题。
5. 修复之前只检查启动器自己的后台权限、漏掉 Termux 后台常驻状态的问题。
6. 修复新手第一次启动酒馆时，部分机型更容易因为 Termux 退到后台被系统打断、却缺少明确提醒的问题。

## [0.9.1-beta3] - 2026-07-03

### Changed

- App 内的更新日志改成更接近正常应用的编号样式，重要内容和新增内容会更靠前显示。
- Termux 命令页会优先显示当前会话的新增输出，并折叠超长模型列表和扩展列表，实时看日志更清楚。
- 横向滑动补了额外的解锁兜底，长时间使用后更不容易突然滑不动。

### Fixed

- 修复 GitHub 更新说明直接显示 `###`、`-` 这类 Markdown 标记，看起来像原始草稿的问题。
- 修复 Termux 日志同步容易夹带 ANSI 颜色码、旧会话内容和重复大段输出，导致实时查看困难的问题。
- 修复部分分区横滑偶发被卡住，滑一段时间后突然无法继续左右切页的问题。

## [0.9.1-beta2] - 2026-07-03

### Changed

- 更新和回退现在都会先弹出统一确认框，先把当前版本、目标版本和当前源说清楚，再执行危险操作。
- 首次安装向导补充了默认版本、默认路径和安装后的下一步提示，新手更容易沿着一条线装到能启动。
- 恢复酒馆备份成功后，会明确提示旧目录保留位置和后续该做的检查，不再让人猜恢复完还要不要补动作。

### Fixed

- 修复版本管理里更新、回退仍沿用双击确认的交互不一致问题。
- 修复恢复完成后提示过短、容易误以为已经自动处理完依赖和善后步骤的问题。

## [0.9.1-beta1] - 2026-07-03

### Changed

- 应用备份前会先显示备份名、时间、大小和恢复目标目录，恢复前更容易确认自己将覆盖什么。
- 版本管理进一步收口，当前 Git 源、官方版本列表和更新/回退按钮状态会一起判断，减少错源和误点。
- 诊断日志补充了更多权限状态和一键体检摘要，反馈问题时不用再手抄那么多信息。

### Fixed

- 修复 Termux 实时同步时旧日志可能反复堆回界面的问题，App 里会优先显示新增内容。
- 修复版本列表和当前 Git 源不一致时仍可能继续更新或回退的风险提示缺口。

## [0.9.0] - 2026-07-03

### Changed

- 新增仓库级 `AGENTS.md` 和项目技能，后续 AI 进入仓库时会先看到发版文案规则和模块化开发规则。
- 发布脚本不再默默生成泛泛而谈的默认公告；发版时必须明确提供正式公告或有意识地使用自动公告。

### Fixed

- 修复发布流程里“先改版本、先提交、最后才发现没写公告”的风险，改成在真正动版本号前先检查公告输入。

## [0.8.33-beta9] - 2026-07-03

### Changed

- 将 Android Lint 纳入 CI，推送和提 PR 时会自动跑静态检查并上传报告。
- 继续补充纯逻辑模块的单元测试，覆盖包源解析、体检报告解析、备份命令编码、Termux 日志增量提取等回归高发点。
- 补齐项目维护基础文件，包括编辑器规范、换行规范、贡献说明、安全说明、Issue/PR 模板和依赖更新配置。

### Fixed

- 修复 `LukoaLauncherScreen` 中已弃用的 `LocalLifecycleOwner` 引用，避免后续升级 Compose/Lifecycle 时继续积累维护警告。
- 补上最基础的 release 构建配置，避免项目只验证 debug、不验证 release 的维护盲区。

## [0.8.33-beta8] - 2026-07-03

### Changed

- 将启动器主界面的大型组合文件拆成导航、启动、版本管理、设置、对话框等模块，降低后续修改风险。
- 为仓库补上 Gradle Wrapper、GitHub Actions CI、基础测试入口和发布脚本防呆。

### Fixed

- 修复拆分界面后跨文件共用组件的可见性问题，恢复项目可编译状态。
- 修复发布脚本在 GitHub Release 不存在时容易中断的问题。
- 修复发布脚本默认把未跟踪草稿文件一起提交的风险。

## [0.8.31] - 2026-07-02

### Fixed

- 修复自动备份设置重启后回到默认值的问题。
- 修复停止酒馆不稳定的问题，改成弹窗确认后再停止。
- 修复镜像源和版本列表错源、缓存错用的问题。
- 修复备份恢复提示里会误导用户的安全备份文案。

### Changed

- 酒馆路径不再由脚本自动接管，只提供候选目录供 App 侧选择。
- 安装预检改成按本次实际使用的仓库去读取和检测。

## [Unreleased]

### Planned

- 继续完善一键体检、失败后一键修复、更新前安全备份、任务断点续做。
