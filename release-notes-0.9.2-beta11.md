## 本次更新

调整酒馆用户管理，移除容易产生误解的重命名操作，并在创建用户前明确说明登录标识限制。

### 修复与优化

- 移除用户重命名入口。
- 新增用户时明确提示登录标识会成为数据目录名。
- 明确说明 SillyTavern 官方没有提供修改登录标识的接口，创建后不能在启动器中重命名。
- 保留用户列表、新增用户和删除账户但保留数据目录功能。

### 说明

- 这是测试版。
- 创建用户前请仔细确认登录标识，避免之后需要手动迁移账户和数据。
