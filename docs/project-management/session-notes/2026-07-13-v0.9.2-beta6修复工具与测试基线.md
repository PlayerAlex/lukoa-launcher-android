# 2026-07-13 v0.9.2-beta6 修复工具与测试基线

这份记录是当前最新交接入口。旧 session notes 作为历史依据保留，其中的版本号、文件行数和下一步建议可能已经过时。

## 当前发布状态

- 分支：`codex/termux-wake-foreground-resume`
- 发布提交：`22f7b0d Release 0.9.2-beta6`
- 版本：`0.9.2-beta6 / versionCode 211`
- GitHub Release：`https://github.com/PlayerAlex/lukoa-launcher-android/releases/tag/v0.9.2-beta6`
- Release 状态：prerelease，APK 资产已确认存在。

## 本版已经完成

- 设置页新增独立“修复工具”分区。
- npm 依赖修复会先移动旧 `node_modules`，安装失败时尽量恢复。
- 主题重置仅在找到可解析且包含兼容主题字段的设置文件时修改，并保留时间戳副本。
- Node.js 内存上限按实例保存，可选 2GB、4GB、6GB，不直接修改 `start.sh`。
- 修复命令要求酒馆停止，并在 App 侧提供二次确认和明确风险提示。

## 测试与覆盖率基线

- 新增备份恢复集成测试、高风险输入边界测试和 Compose UI 交互测试。
- 已接入 JaCoCo Debug JVM 单元测试覆盖率报告。
- 2026-07-13 报告基线：3530 行已覆盖、13358 行未覆盖，行覆盖率约 20.9%。
- 报告命令：`.\gradlew.bat --no-daemon :app:jacocoDebugUnitTestReport`
- HTML：`app/build/reports/jacoco/debugUnitTest/html/index.html`

## 后续优先事项

1. 在真实 Termux 环境验证依赖修复成功、失败恢复和磁盘空间不足场景。
2. 使用多个 SillyTavern 版本验证主题设置文件位置与字段兼容性。
3. 为修复脚本补可独立执行的 shell 行为测试，避免只能依赖 Android 构建发现问题。
4. 再评估扩展隔离管理；删除扩展必须先移动到可恢复目录，不能直接递归删除。
